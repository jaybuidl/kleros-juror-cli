Q12. Agreed, no PRIVATE_KEY env var

Q13. Good catch, yes it was slip, I did mean `@kleros/kleros-v2-contracts`, there's no need for `@kleros/kleros-sdk`

Q14. Agreed A then C then B

Q15. ok

Q16. yes this project's `docs/research/` governs. The other research artifacts where created before Kleros agentkit was made, so the PRD obsolete, the best practices are still relevant although we learned newer insights since building Kleros agentkit
